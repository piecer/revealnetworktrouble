# Graph-first topology

Scope: detached worktree at b27c729; frontend only, no deployment or release changes.

1. RED→GREEN: native collapsed semantic inspector after canvas, explicitly costed in DOM plan; preserve progressive chunk/ownership limits.
2. RED→GREEN: fixed graph-unavailable guidance (never exception prose).
3. RED→GREEN: fit finite projected bounds in both modes, preserving bounded interaction and sensible singleton/reset behavior.
4. Replay committed fixtures and captured real report on private worktree server in Chromium (1440/375, 2D/3D), record screenshots, pixels, visibility and no additional mode/reset requests.
5. Run full web tests/syntax, make test/build; hand off unstaged changes and exact evidence for parent review.
